## Form trả lời bình luận giờ được thông báo và quản lý focus

**Vấn đề**

`comments/_item.blade.php:64` ẩn form trả lời bằng `style="display:none"`, và JS chỉ đổi thuộc tính `display`:

```js
document.getElementById('reply-form-' + id).style.display = 'block';
```

Ba vấn đề chồng lên nhau:

1. **Không thông báo (WCAG 4.1.3 Status Changes, Level AA)** — người dùng máy đọc màn hình bấm "Trả lời" và vùng nội dung thay đổi, nhưng không có gì báo cho họ biết.
2. **Focus không di chuyển** — sau khi bấm, focus vẫn ở nút "Trả lời"; người dùng bàn phím phải tab mò qua toàn bộ comment mới chạm tới textarea.
3. **Nút "Hủy" bỏ rơi focus** — ẩn form xong, focus vẫn nằm trong vùng vừa bị ẩn.

**Cách khắc phục**

| Thay đổi | Lý do |
|---|---|
| `aria-expanded` + `aria-controls` trên nút "Trả lời" | Báo trạng thái mà không cần mở |
| Handler chung `setReplyOpen(btn, open)` | Trạng thái và visibility không thể lệch nhau |
| Mở → focus vào textarea, đóng → trả focus về nút | Luồng bàn phím không bị đứt |
| Container `role="group"` + `aria-label` | Khi mở, form là landmark có tên |
| Live region `role="status" aria-live="polite"` | Thông báo mở/đóng (render server-side, JS chỉ ghi text) |
| Rule `.sr-only` riêng trong partial | Bootstrap không load `.sr-only` trong app này — **clip, không `display:none`** |

**Chi tiết kỹ thuật quan trọng**: vùng live phải được render server-side. JS chỉ ghi text vào nó — nếu tạo bằng JS thì người dùng tắt JS (hoặc tooling đọc DOM tĩnh) sẽ không bao giờ thấy. Rule ẩn phải **clip** (`position:absolute; clip:rect(0,0,0,0)`), tuyệt đối không `display:none` — cái sau gỡ vùng khỏi accessibility tree và làm tắt tiếng toàn bộ điểm của fix.

**Kiểm chứng**

- `CommentReplyFormA11yTest`: 4 test / 15 assertion.
- **Kiểm soát âm**: stash fix, giữ nguyên test → **4 đỏ / 0 xanh**.
- Suite SQLite: **804 passed / 5 skipped / 4026 assertions** (từ 800/4011).
- Pint: pass.

Không đổi action POST `comments.store`, validation, `parent_id`, class CSS, hay cấu trúc đệ quy. Nút like (#408) và edit/delete (#388) không bị động chạm. Không phơi key.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
